name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 */3 * *'  # 每3天运行一次
  workflow_dispatch:        # 允许手动触发

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s \
            -H "apikey: sb_publishable_LjlNWK0x6W80u6t_jdIXsw_bFIfsiCd" \
            -H "Authorization: Bearer sb_publishable_LjlNWK0x6W80u6t_jdIXsw_bFIfsiCd" \
            "https://uxqxbngvctkcrzcxiqff.supabase.co/rest/v1/" \
            && echo "Supabase pinged successfully"
